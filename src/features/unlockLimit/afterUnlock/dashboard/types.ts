export enum ACTION {
    DASHBOARD = "DASHBOARD",
    PROFILE = "PROFILE",
    WITHDRAW = "WITHDRAW",
    REPAYMENT = "REPAYMENT",
    MENU = "MENU"
  }

  
  export type AssetsPayload = {
    value: string;
    widgetId: string;
  };

  